# get_jobs 浏览器插件迁移计划

## 目标

将 `get_jobs` 改写为 Manifest V3 浏览器插件；猎聘闭环已基本收束，当前开始迁移智联，BOSS 继续保持关闭。

## 范围约束

- 新工程：`D:\Github\get_jobs-extension`
- 现有 `D:\Github\get_jobs` 只作为行为与数据模型参考，不在本阶段继续扩展 Java 自动化。
- 当前平台范围为猎聘与智联；BOSS 不实现，统一插件合并暂缓。
- 首期验收以“用户已登录的真实 Chrome 标签页”为运行环境。

## 阶段

| 阶段 | 状态 | 验收标准 |
|---|---|---|
| 1. 梳理猎聘现有闭环 | complete | 明确登录、检索、过滤、投递、成功确认、记录的数据流与选择器 |
| 2. 建立 MV3 插件骨架 | complete | 插件可构建、可加载、可识别猎聘标签页 |
| 3. 迁移猎聘配置与岗位解析 | complete | 可保存配置并从当前页面解析岗位信息 |
| 4. 实现单岗位投递闭环 | complete | 点击投递后等待明确成功/失败/验证码结果，再写入记录 |
| 5. 实现任务状态与可恢复性 | complete | 可开始、停止，刷新或后台休眠后不重复投递 |
| 6. 自动化验证与本机验收准备 | in_progress | 构建/测试通过，给出真实账号单岗位验收步骤 |
| 7. Git 提交 | complete | 按 yuwk 历史风格生成并自动提交 |
| 8. 增加网页可见入口 | complete | 猎聘页面显示 Get Jobs 悬浮按钮，点击可打开侧边栏 |
| 9. 支持悬停后动态沟通按钮 | complete | 未接触岗位可在投递时激活招聘者区域并识别“聊一聊” |
| 10. 设计简历与 AI 招呼链路 | complete | 已核清原版与 boss-helper 机制，确定 AI 草稿、确认发送与简历回执的分阶段路径 |
| 11. 实现 AI 招呼与简历闭环 | complete | AI 生成草稿，预览确认默认开启且可关闭，页面发送消息与简历并分别确认回执 |
| 12. 真实账号单岗位验收 0.2.0 | in_progress | 重新加载扩展后完成一次“草稿确认 → AI 消息回执 → 简历卡片回执”验收 |
| 13. 修复 AI 配置保存与草稿瞬退 | complete | AI 区域可独立保存，生成草稿自动使用当前表单配置，并清理旧失败状态误导 |
| 14. 增加 AI 请求超时配置 | complete | AI 模块可配置 10–600 秒请求超时，默认 120 秒，保存后真实控制后台请求取消时间 |
| 15. 修复聊天窗口岗位绑定与后续发送 | complete | 新沟通窗口可与原岗位稳定绑定，随后发送 AI 文本并确认或去重简历回执 |
| 16. 修复猎头岗位聊天绑定 | complete | 截断岗位名与猎头/用人公司展示差异下仍能安全绑定并进入 AI 发送步骤 |
| 17. 顺序投递当前页面 | complete | 用户一次确认后按当前快照顺序处理未联系岗位，使用可配置随机间隔并可安全停止 |
| 18. 兼容附件简历确认弹窗 | complete | 点击发简历后自动选择已有附件并点击明确的“立即投递”，随后等待本人简历卡片回执 |
| 19. 增加顺序投递账号安全护栏 | complete | 已提供批次/每日上限、连续成功冷却、持久化当日计数与异常熔断，不实现风控规避或验证码绕过 |
| 20. 完全自定义 AI 提示词与 150 字稳定约束 | complete | 已允许用户控制完整业务提示词模板，并在生成阶段自动压缩/收口超长招呼语，发送前仍保留硬校验 |
| 21. 修复 GLM 空响应误判 | complete | 已移除可能截断推理模型最终正文的固定低 token 上限，区分空响应与“需人工判断”，并补充回归测试 |
| 22. 增加单岗位动作节奏控制 | complete | 已在页面移动、建立沟通、发送招呼、发送/确认简历与关闭聊天等阶段加入可配置随机稳定等待，并适度缩短岗位间等待 |
| 23. 评估首页注入式主界面 | complete | 已确认可实现；推荐 Shadow DOM 固定抽屉承载扩展 iframe，风险中低，不直接挤压猎聘布局，也不修改投递业务代码 |
| 24. 实现猎聘首页注入式主界面 | complete | 0.3.0 已用 Shadow DOM 固定抽屉承载现有 React 扩展页，悬浮入口与工具栏图标均切换抽屉，Side Panel 运行依赖已移除且自动化验收通过 |
| 25. 扩展抽屉与 AI 诊断/兜底 | complete | 0.3.1 已将抽屉扩大至 520px 并修复表单横向溢出；提供最近 20 次脱敏 POST 诊断、显式完整日志开关和可配置兜底招呼语，页面发送结果不明时仍禁止自动重试 |
| 26. 评估与 boss-helper-v2 合并 | complete | 已确认技术可行；推荐以 BOSS/WXT 为统一宿主，首期保留猎聘 React iframe 与独立状态机，先解决后台存储访问、扩展身份数据迁移和许可证，再做 Chrome 双平台验收；智联延后 |
| 27. 梳理智联页面与原版闭环 | complete | 已明确首版为“立即投递默认简历”，并提取岗位、成功/失败/验证/上限候选；当前无 Chrome 页面样本，选择器需真实账号验收 |
| 28. 建立智联平台适配骨架 | complete | 0.4.0 已包含独立 Manifest 入口、Content Script、后台任务/历史、页内 React 界面，猎聘入口保持隔离 |
| 29. 实现智联单岗位闭环 | in_progress | 一次点击后自动选择配置的简历、提交并仅在明确成功后关闭结果页；不再弹插件二次确认 |
| 30. 实现智联批次与安全护栏 | complete | 当前页顺序处理、动作随机等待、独立配额、长冷却、异常熔断与停止传播均已实现；使用前仍要求先做单岗位真实验收 |
| 31. 自动化验证与提交 | complete | 46 项测试、类型检查、0.4.0 生产构建通过，源码、构建配置与 README 已按 yuwk 规范拆分自动提交 |
| 32. 适配智联新版登录与岗位 DOM | complete | 0.4.1 已在真实登录页面只读确认登录为 true、岗位与按钮各 20 个，49 项测试、类型检查和生产构建通过 |
| 33. 智联 0.5.0 自动简历与顺序投递 | complete | 55 项测试、类型检查和生产构建通过，源码、版本与文档已按 yuwk 规范自动提交；待用户重新加载后做一个真实岗位验收 |
| 34. 简化智联默认简历闭环 | complete | 0.5.1 已删除插件简历选择/二次提交，只保留“立即投递 → 明确回执 → 成功关闭”，批次同步复用且回归通过 |

## 关键设计决策

- 使用 Manifest V3、TypeScript 和 React。
- 页面 DOM 操作由猎聘 Content Script 执行。
- Service Worker 只做任务协调、消息路由和持久化，不依赖常驻内存。
- 配置使用 `chrome.storage.local`，岗位与投递记录使用 IndexedDB。
- 只有检测到平台明确成功结果后才记录为已投递。
- AI 招呼首版采用“生成草稿、预览确认、页面发送”，不把私有接口直发与自动附件混入首个可验收闭环。
- 简历发送必须单独等待猎聘明确回执，不能把“建立沟通成功”等同于“简历已发送”。
- 预览确认开关默认开启；关闭仅跳过人工确认，不跳过 AI 草稿校验、消息回执和简历回执。
- 统一插件以 `boss-helper-v2` 的 WXT/Vue 仓库为宿主，猎聘 React iframe 作为受支持的过渡入口保留。
- BOSS 与猎聘保持平台独立 Content Script、配置 namespace、IndexedDB 和投递状态机，只共享唯一后台注册器与少量基础设施。
- 统一插件合并暂缓；智联直接接入现有 `get_jobs-extension`，通过平台隔离复用 AI、任务和安全基础设施。
- 智联首个里程碑只做单岗位闭环，批量投递必须在明确回执稳定后再开放。
- 智联要求用户预先配置平台默认简历；扩展只点击岗位“立即投递”，不打开、选择或二次提交简历控件。
- 只有明确成功才允许关闭结果弹窗或本次新开的结果标签页；未知、失败、验证和达到上限均保留现场并熔断批次。

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 在新目录执行 `git status` 返回“not a git repository” | 1 | 新工程尚未初始化 Git；验证完成后执行 `git init` 再检查和提交 |
| 中文提交脚本拒绝一次性自动暂存多文件范围 | 3 | 根据脚本 scope 判定逐文件拆分，并完成所有模块提交 |
| Browser Relay 无法连接本机中继服务 | 1 | CLI 与扩展文件正常，但 127.0.0.1:18795 未响应；不阻塞本地构建，改由用户重新加载扩展后做可见性验收 |
| AI 测试的 `vi.fn` 被推断为无参函数，TypeScript 无法读取第二个调用参数 | 1 | 为 mock 显式声明 FetchLike 兼容参数，再重新执行串行检查 |
| PowerShell 中组合 `rg` 正则时引号转义导致表达式未闭合 | 1 | 改用多个 `-e` 固定模式执行定向检索，不重复使用该组合表达式 |
| 0.2.3 README 补丁定位文本与现有文档不一致 | 1 | 先读取 README 功能说明段落，再按真实上下文拆分补丁 |
| 浏览器只读 evaluate 直接引用 `HTMLTextAreaElement` 不可用 | 1 | 改为从实际 textarea 的原型链读取 value setter，不依赖沙箱全局构造器 |
| 风险提示补丁按错误的分支结构定位失败 | 1 | 读取 `detectVerificationEvidence()` 真实代码后，改为替换现有组合条件，不重复原补丁 |
| 间隔归一化测试仍拿完整批量配置比较 | 1 | 将断言收窄为函数真实返回的最短/最长间隔字段，再串行重跑完整检查 |
| 0.2.7 样式与文档组合补丁未命中 `.privacy-note` 上下文 | 1 | 先读取真实样式位置，再拆分样式和文档补丁，不重复原组合补丁 |
| 动作等待补丁为 `closeChatWindow` 留下重复函数声明 | 1 | 定向删除旧签名并补齐新参数注释，再继续修改调用链 |
| 阶段 23 记录补丁引用了被截断的进度文本 | 1 | 改为读取文件尾部并使用真实完整文本定位，不重复使用截断上下文 |
| PowerShell 使用嵌套范围数组输出代码行时报 `Argument types do not match` | 1 | 后续改用 `Select-Object -Skip/-First` 或单一整数范围，避免数组类型推断问题 |
| Windows 下把 `*.ts` 通配符作为 `rg` 路径参数导致路径语法错误 | 1 | 只向 `rg` 传目录并用 `-g '*.ts'` 过滤，避免 PowerShell/Windows 路径通配符差异 |
| 中文提交脚本拒绝把三份规划文档作为一个自动暂存范围 | 1 | 遵循脚本单一范围规则，按 findings、progress、task_plan 三个文件依次提交 |
| 新增页内抽屉测试沿用 Vitest 默认 Node 环境，`document` 不存在 | 1 | 在该测试文件显式声明 jsdom 环境，再继续串行执行完整检查 |
| Chrome 控制通道不能接管 `chrome://extensions` 内部页面 | 1 | 不绕过浏览器安全边界；保留静态/自动化验收并要求用户手动重新加载扩展后做真实页面可见性验收 |
| 诊断测试回调直接返回 `Array.push` 的数字结果，不符合 `void` 回调签名 | 1 | 改为带函数体的无返回值回调后重新执行完整检查 |
| Browser Relay 无法连接本机中继服务（智联阶段） | 1 | 记录 `127.0.0.1:18795` 不可达；尝试启动本机中继，若 Chrome 扩展未连接则先完成静态适配与测试，不执行真实投递 |
| 智联解析测试把地区误识别为学历 | 1 | 放弃受前置 DOM 节点影响的 `:nth-child()`，改为收集全部其它信息项后按语义顺序读取 |
| 智联跨标签回执可空值未收窄 | 1 | 先显式确认 `response.data` 存在，再判断 outcome，避免空数据被误当作非 unknown |
| PowerShell 解析提交前组合 `rg` 正则失败 | 1 | 拆成多个 `-e` 固定模式并避免在双引号命令中嵌套未转义引号 |
| Windows 下将 `package*.json` 直接作为 `rg` 路径参数失败 | 1 | 后续显式传入 `package.json` 与 `package-lock.json`，不使用路径通配符 |
| 中文提交脚本拒绝共享类型、配置、解析器和测试的组合暂存 | 1 | 安全撤销仅本轮暂存标记，按文件逐一使用 yuwk 作者提交，不改动工作区内容 |
| 中文提交脚本拒绝共享类型、智联解析器和测试的组合暂存 | 1 | 只撤销本轮暂存标记，按解析、类型、页面、后台、界面、构建和文档进一步拆分提交 |
| 智联登录未知状态测试被全页“登录”文本误判 | 1 | 顶部导航不存在时直接返回 null，仅在已定位的智联 Header 内判断明确登录入口 |
| 记录登录修复的组合补丁格式无效 | 1 | 去除空补丁段，使用带真实上下文的单一组合补丁重新应用 |
