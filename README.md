# Get Jobs Extension

`get_jobs` 的 Manifest V3 浏览器插件改写版。第一阶段只实现猎聘单岗位投递闭环，BOSS 保持关闭，智联在猎聘真实账号验收后迁移。

## 当前能力

- 直接使用用户当前 Chrome 标签页和猎聘登录态，不启动 Playwright 浏览器。
- 在猎聘页面右下角显示 `Get Jobs 助手` 悬浮入口，点击即可打开侧边栏。
- 在侧边栏识别当前猎聘搜索结果页的岗位卡片。
- 由用户明确选择一个岗位后执行一次“聊一聊”。
- 点击后等待聊天窗口或平台成功提示，只有明确成功才记录为新投递。
- 将“继续聊”单独记录为此前已联系，不计入本次新投递。
- 遇到验证码、登录失效、页面刷新或用户停止时安全中止。
- 配置和任务状态保存在 `chrome.storage.local`，岗位与尝试记录保存在 IndexedDB。

## 本地构建

```powershell
npm install
npm run check
```

构建产物位于 `dist`。

## 加载插件

1. 打开 `chrome://extensions`。
2. 开启“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择本项目的 `dist` 目录。
5. 已加载过旧版本时，必须在扩展卡片上点击一次“重新加载”。
6. 刷新已经打开的猎聘页面。
7. 点击页面右下角的 `Get Jobs 助手` 悬浮按钮，或点击 Chrome 工具栏中的插件图标打开侧边栏。

## 猎聘单岗位验收

1. 在真实 Chrome 中登录猎聘，并打开岗位搜索结果页。
2. 手动关闭已有聊天窗口或验证弹窗。
3. 在插件侧边栏点击“重新识别”。
4. 选择一个用于验收的岗位，点击“投递这一个”。
5. 确认猎聘聊天窗口出现，随后检查猎聘沟通记录中是否存在该岗位。
6. 回到侧边栏确认记录为“新投递成功”；若按钮原本是“继续聊”，应记录为“此前已联系”。

## 安全边界

- 插件不会绕过验证码、安全验证或平台限流。
- 页面刷新或浏览器重启后不会自动重复点击，会将任务标为“已中止”并要求人工核对。
- 当前仅完成单岗位闭环，不包含批量翻页、自动连续投递、BOSS 或智联适配器。
