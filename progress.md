# 进度记录

## 2026-08-05

- 确认迁移优先级：猎聘第一，智联第二，BOSS 保持关闭。
- 创建独立插件工程目录与文件化迁移计划。
- 当前阶段：梳理现有猎聘闭环。
- 完成两路只读代码梳理：Worker 闭环、配置/数据库/前端模型。
- 明确旧猎聘逻辑存在“关闭聊天窗失败也标记投递”的宽松判定，插件版将改为明确成功后落库。
- 阶段 1 完成，进入 Manifest V3 插件骨架实现。
- 创建 Manifest V3、React/TypeScript 侧边栏、Service Worker、猎聘 Content Script 和独立构建脚本。
- 实现岗位卡片解析、单岗位选择、“聊一聊/继续聊”区分、成功证据等待、验证码/登录失效/停止处理。
- 实现 chrome.storage 任务状态与 IndexedDB 投递记录，页面或浏览器重启时阻止自动重复点击。
- 添加解析单元测试、加载说明和真实账号单岗位验收清单。
- 第一轮 `npm run check` 通过：3 个测试通过，TypeScript 检查与生产构建成功，npm 审计 0 漏洞。
- 产物检查时发现新工程尚未初始化 Git，已记录并将在提交阶段初始化。
- 独立只读审查发现 5 项任务竞态/持久化风险；已全部修正。
- 第三轮 `npm run check` 通过：3 个测试、类型检查和生产构建全部成功。
- 第二轮独立审查逐项确认 taskId、状态锁、停止竞态、迟到结果、看门狗和 IndexedDB 修复有效，未发现新的运行时阻断问题。
- 阶段 2 至 5 代码完成；阶段 6 仍等待真实 Chrome 猎聘账号的单岗位业务验收。
- 已初始化 `main` 分支；提交技能判定一次性范围过大，改为按模块拆分提交。
- 已按 yuwk 中文 Conventional Commit 规则完成构建、共享、后台、内容脚本、侧边栏和文档的模块化提交。
- 收到真实 Chrome 反馈：插件已启用但页面无可见入口；新增阶段 8 修复网页入口可发现性。
- 新增 Shadow DOM 隔离的猎聘页面悬浮按钮，并通过 Service Worker 打开对应标签页侧边栏。
- 版本升级为 0.1.1，README 补充“重新加载扩展 + 刷新猎聘页面”步骤。
- Browser Relay 本机服务未运行，无法代替用户直接检查现有标签页；已记录为非构建阻断项。
- 通过现有 Chrome 控制通道确认当前页面域名为 `c.liepin.com`、旧版悬浮入口未注入，但岗位卡片选择器可匹配 40 个元素。
- 扩展 Manifest 已改为覆盖全部猎聘子域，这是本次页面无控件的直接修复。
- 收到第二次真实 Chrome 反馈：只能投递已经显示“继续聊”的岗位；新增阶段 9 处理悬停后动态挂载的“聊一聊”。
- 真实页面验证：第一张卡片悬停后出现“继续聊”，第二张未联系卡片悬停后出现“聊一聊”，按钮类均为 `ant-btn ant-btn-round ant-btn-primary`。
- 0.1.2 已取消无按钮岗位的提前禁用，并在投递时向卡片与招聘者区域发送完整 pointer/mouse 悬停事件序列，等待 React 动态挂载沟通按钮。
- 第三次验收确认已能建立沟通，但未发送简历且仍使用猎聘默认招呼；开始核验原版能力与 boss-helper 的 AI/发送机制。
- 核验确认原版猎聘只触发“聊一聊”，依赖猎聘账号默认/自定义招呼，不调用 AI，也不发送简历附件。
- 核验确认 boss-helper V2 的 AI 只生成草稿且硬性禁止自动消息发送；其私有接口自动化仅用于 BOSS 发起沟通，附件上传链路未启用。
- 阶段 8、9 已由真实 Chrome 验收完成；阶段 10 完成设计，后续按“AI 草稿与确认发送 → 简历独立回执 → 私有接口实验”的顺序推进。
- 用户确认阶段 11 范围：AI 生成草稿 → 默认开启、可关闭的预览/编辑确认 → 页面发送消息 → 单独发送简历 → 分别等待成功回执。
- 已完成四路只读代码审计，并在当前已登录猎聘页面只读确认聊天输入框、“发送”、“发简历”和历史简历卡片均存在；未产生外部发送行为。
- 完成 0.2.0 第一版实现：OpenAI 兼容配置与按域授权、API Key 隔离存储、默认开启的草稿预览编辑、页面消息发送、独立简历发送及三阶段回执。
- `npm run check` 通过：AI 与岗位解析共 7 个测试通过，TypeScript 检查和生产构建成功。
- 两路独立复核发现并修正标签页切换误投、点击后未知结果误报失败、聊天岗位未绑定、AI URL 凭据和密钥清除等风险；正在执行最终回归。
- 0.2.0 最终 `npm run check` 通过：7 个测试、TypeScript 检查和生产构建均成功；阶段 11 完成，进入真实账号单岗位验收。

## 2026-08-06

- 真实验收反馈：AI 表单已输入模型和 API Key，但状态仍显示“Key 未配置”；点击“生成草稿”后按钮短暂进入忙碌并立即恢复，提示“请先配置 AI 模型和 API Key”。
- 截图同时显示顶部保留上一轮投递的“失败”和“点击后未在限定时间内检测到明确成功结果”，与本次草稿生成错误混在一起，造成状态误导。
- 第二张验收截图改用 `http://127.0.0.1:3001/v1` 本机 OpenAI 兼容代理，仍显示“Key 未配置”，进一步确认故障与服务商无关，发生在表单密钥尚未持久化的前置流程。
- 已为 AI 模块增加独立“保存 AI 配置”按钮与 Key 三态提示，并让“生成草稿”自动先保存当前配置；后台保存响应会回读真实密钥状态。
- 已区分草稿活动状态与历史投递任务状态，历史失败改为“上次失败”，避免误导为本轮草稿错误。
- 已修正 OpenAI 兼容地址拼接，覆盖本机 `/v1` 与智谱 `/api/paas/v4`；首轮 `npm run check` 通过 7 个测试、类型检查和生产构建。
- 版本升级至 0.2.1；最终 `npm run check` 再次通过，阶段 13 完成，下一步需在 Chrome 扩展页重新加载后继续阶段 12 的真实账号单岗位验收。
- 用户反馈本机 GLM 生成速度可能超过默认 30 秒，开始增加可持久化的 AI 请求超时配置。
- 已将 AI 超时默认值提升至 120 秒并提供 10–600 秒配置；后台读取、保存和请求计时统一使用归一化后的值，旧配置自动回退至新默认值。
- 第一轮 `npm run check` 通过 8 个测试、类型检查和生产构建，随后补充边界值与旧配置默认值测试。
- 版本升级至 0.2.2；最终 `npm run check` 通过 9 个测试、类型检查和生产构建，阶段 14 完成。
- 真实单岗位验收已成功打开新聊天并触发猎聘默认招呼，但插件报“未检测到与所选岗位匹配的聊天窗口”，因此 AI 消息和简历步骤均未执行；开始修复岗位绑定校验。
- 通过当前已登录 Chrome 只读确认目标聊天岗位、公司、发送方消息与简历节点：地区后缀确实被聊天头移除，AI 草稿不存在，但聊天内已出现平台产生的简历卡片；后续修复需同时防止重复发送简历。
- 已实现完整标题优先、核心标题与公司联合降级匹配；AI 文本与简历回执轮询改为重新获取当前聊天节点，并在已有简历卡片时跳过重复点击。
- 第一轮 `npm run check` 通过 11 个测试、类型检查和生产构建；版本升级至 0.2.3。
- 最终 `npm run check` 再次通过 11 个测试、类型检查和生产构建；阶段 15 完成，等待重新加载扩展后的新岗位真实验收。
- 新一轮真实验收的猎头岗位仍只产生平台默认招呼，没有 AI 消息；同时用户要求增加当前页面全部岗位的顺序投递与随机间隔控件。
- 当前 Chrome 只读复现确认 0.2.3 仍失败的根因是岗位解析器错误地把薪资、经验和学历并入 `jobTitle`，并非本轮 AI 草稿生成或发送按钮逻辑先失败。
- 已提高纯岗位标题节点的解析优先级，并让聊天绑定兼容 0.2.3 留下的“岗位名【地区】+薪资经验学历”旧快照；新增两项回归测试覆盖真实猎头岗位结构。
- 已实现当前页顺序投递入口、二次确认、5–300 秒随机间隔配置、已联系岗位跳过、运行进度与可中断停止；任一岗位回执不明确时整批立即停止。
- 首轮 0.2.4 `npm run check` 通过：3 个测试文件共 15 项测试、TypeScript 检查和生产构建均成功；开发过程未触发任何真实岗位投递或消息发送。
- 版本升级至 0.2.4；最终 `npm run check` 再次通过 15 项测试、类型检查和生产构建，`dist/manifest.json` 版本核对为 0.2.4，阶段 16、17 完成。

## 2026-08-08

- 真实验收已成功进入简历阶段，但页面停留在“选择附件简历”弹窗；弹窗内已有默认附件且需要再次点击“立即投递”。
- 开始阶段 18：只读核对弹窗 DOM，扩展简历确认状态机并补充自动化回归；开发检查不点击真实“立即投递”。
- Chrome 只读核验已确认“立即投递”是真实 `BUTTON` 且附件 radio 已默认选中；无需操作附件选择，仅需扩充安全确认白名单并继续等待原有简历卡片回执。
- 已将简历弹窗候选解析抽为可测试共享模块；仅在附件已选、确认文案精确命中且全页只有一个安全候选时返回按钮，内容脚本再执行点击。
- 首轮回归通过：4 个测试文件共 19 项测试、TypeScript 检查和生产构建均成功；新增测试覆盖“立即投递”、未选附件、多候选和聊天窗口误识别。
- 独立只读复核未发现阻断项，确认按钮点击本身不会被当作成功；根据复核补充禁用按钮和近似文案负例，继续坚持精确白名单。
- 版本升级至 0.2.5；最终 `npm run check` 通过 4 个测试文件共 20 项测试、TypeScript 检查和生产构建，`dist/manifest.json` 版本核对为 0.2.5，阶段 18 完成。
- 用户要求为顺序投递增加防封号措施；开始阶段 19，范围限定为合规限速、配额、长冷却和异常熔断，不加入指纹伪装、验证码绕过或规避平台检测。
- 用户认为先合并 BOSS 与猎聘的收益较小，决定暂停统一插件路线，开始执行智联接入。
- 已确认 `get_jobs-extension` 工作区干净；`boss-helper-v2` 仍保留用户原有的 ZIP 删除，本轮不在该仓库写入。
- 开始阶段 27：先核对原 Java 智联实现与当前插件平台耦合点，再按“平台骨架 → 单岗位明确回执 → 批次安全护栏”顺序迁移。
- 已确认当前构建只有猎聘 Content Script，需为智联新增独立构建产物和 Manifest 匹配范围；不复用同一个页面脚本入口。
- 已提取原 Java 智联候选选择器与结果文案，但将只作为线索，后续必须用当前真实页面进行只读核验。
- 智联页面只读核验首次尝试被本机 Browser Relay 未启动阻断；已记录环境错误，后续不以此为理由触发任何未经确认的真实页面操作。
- 已启动 Browser Relay 本机服务，但 Chrome 暂无附加标签页；继续推进智联静态适配、解析测试和平台骨架，不执行外部投递。
- 已完成平台耦合初查：采用新增智联 Content Script/解析器/消息路由的增量方案，避免立即重写猎聘约 1,300 行 UI 与现有状态机。
- 已把智联首个闭环收窄为平台真实支持的“立即投递 + 默认简历 + 明确申请回执”；不把猎聘 AI 聊天流程错误复制到智联。
- 已新增智联岗位、页面、任务、回执和尝试记录类型，并建立独立 DOM 解析器与回执文本判定测试；成功判定只接受“申请成功/投递成功”。
- 已新增 `zhilian-content.js` 独立入口、智联页内抽屉、单岗位二次确认、短随机点击前等待、停止能力及当前页/由列表页打开的新标签页回执轮询。
- 已新增智联独立任务存储、看门狗和投递历史；猎聘与智联互相阻止并发任务，避免跨平台同时触发外部申请。
- Manifest 与构建版本已升级至 0.4.0，新增智联域权限和仅智联域可访问的抽屉资源；猎聘原 Content Script 保持独立。
- 首次 `npm run check` 的 46 项测试中 45 项通过；唯一失败暴露学历字段使用 `:nth-child()` 受前置节点影响，已改为信息项数组读取，并修复跳过装饰卡时的动作前卡片索引错位。
- 第二次检查 46 项测试全部通过，TypeScript 随后发现跨标签回执可能为空；已修正存在性判断，防止空响应被误当作有效回执。
- 完整检查已通过；提交前组合检索命令因 PowerShell 引号解析失败，已记录并改用拆分固定模式，不重复原命令。
- 静态审查确认 Manifest 生成了两个平台独立 Content Script，智联未知结果不重试且不自动关闭结果页；已同步 package-lock 根版本至 0.4.0。
- README 已更新智联 0.4.0 能力、加载步骤和严格单岗位验收流程；智联抽屉首次加载默认展开，保持与猎聘一致的可发现性。
- 提交前补强智联回执归因：点击前快照旧提示和既有结果标签，防止上一岗位的成功证据误计入本次任务。
- 最终代码回归通过：7 个测试文件共 46 项测试、TypeScript 检查和 0.4.0 生产构建全部成功；阶段 28 完成，阶段 29 等待真实账号单岗位回执验收。
- 自动提交首次被中文提交脚本判定暂存范围过大；已安全撤销本轮暂存标记，准备按更小模块依次提交。
- 已按中文提交技能要求拆分完成智联解析/测试、共享类型、Content Script、后台、界面、构建、Manifest、版本和 README 提交；阶段 31 完成。
- 当前未完成项只有阶段 29 的真实账号单岗位业务验收，以及验收稳定后才启动的阶段 30 智联批次安全护栏。
- 收到智联 0.4.0 首轮真实验收截图：页面明确已登录，但助手显示“未登录”；同时确认新版岗位卡片已偏离原 Java DOM。
- 新增阶段 32，先只读核验真实 DOM，再同时修复登录和岗位解析，避免逐个选择器反复验收。
- 已通过已登录 Chrome 只读核验：定位到智联真实登录类名，且确认当前页 20 个岗位的卡片、标题和投递按钮主选择器仍有效；本轮只需修复登录判断并增加诊断/测试。
- 已将登录判断抽到共享解析器：先识别 `.home-header__c-login` 下的头像+账号名或“个人中心/我的简历/退出”组合，再仅在顶部导航中寻找明确登录入口；全页隐藏登录文本不再造成假阴性。
- 智联界面现在区分“已登录/未登录/登录状态待确认”，版本升级至 0.4.1，并新增真实账号结构、未登录结构和未知状态回归测试。
- 首轮 0.4.1 检查有 48/49 项通过；失败项揭示无 Header 时仍回退全页搜索登录文本，已改为无顶部导航直接返回待确认。
- 首次记录该修复的组合补丁因空补丁段格式无效，未产生代码修改；已改用有效上下文重新应用。
- 0.4.1 完整回归通过：7 个测试文件共 49 项测试、TypeScript 检查和生产构建均成功。
- 修复规则已在当前真实智联标签页只读运行并返回 `loggedIn=true`；岗位卡片和“立即投递”按钮均识别 20 个，首张岗位字段抽样正确。阶段 32 完成。
- 收到智联简历选择弹窗截图并开始重做阶段 29：单岗位入口改为一次点击直接执行，自动选择附件简历、等待明确回执并只在成功时关闭结果页；随后实现阶段 30 当前页顺序投递与安全护栏。
- 已确定保守边界：不点击平台级“每次投递默认发送该简历”，多附件无唯一匹配、结果未知、验证或达到上限时停止并保留现场；开发期间不触发真实岗位投递。
- 通过当前已登录 Chrome 做了只读快照；弹窗已经关闭，未为提取选择器重新点击“立即投递”。后续以截图语义和严格唯一匹配实现，并由用户用一个新岗位完成真实验收。
- 已完成智联单岗位直投状态机、列表页/新标签页简历工作流、明确成功关闭、独立配置/每日额度/长冷却和当前页顺序投递入口；插件二次确认已移除。
- 新增简历弹窗回归测试，覆盖附件优先、按名称选择、多附件歧义、近似提交文案和成功弹窗唯一关闭；首轮 8 个测试文件共 55 项测试全部通过。
- 误执行不存在的 `npm run typecheck` 一次，确认项目实际以 `npm run build` 内的 `tsc --noEmit` 为类型检查入口；随后生产构建已通过。
- 首轮完整 `npm run check` 在 0.5.0 下通过 8 个测试文件共 55 项测试、TypeScript 检查和生产构建；静态复核继续补充智联频控/账号异常熔断，以及按钮先变“已投递”时等待成功弹窗后再尝试关闭。
- 最终 `npm run check` 再次通过：8 个测试文件共 55 项测试、TypeScript 检查和 0.5.0 生产构建成功；`dist/manifest.json` 已核对为 0.5.0。阶段 30 代码完成，真实环境仍按“先单岗位、后批次”验收。
- 中文提交技能拒绝共享层多文件组合暂存后，已仅撤销本轮暂存标记并按文件拆分；智联 0.5.0 的共享配置/测试、简历解析、Content Script、后台、界面、构建版本和 README 均已使用 yuwk 作者自动提交。
- 阶段 33 完成；工作区剩余三份规划记录待按同一规范提交。开发全程未点击真实“立即投递”或“投递简历”。
- 用户确认智联平台默认简历已配置，“立即投递”本身即发送简历；开始阶段 34，移除多余的简历选择和二次提交逻辑。
- 已删除智联简历模式/附件名称配置和弹窗解析器，状态机改为“随机等待 → 立即投递 → 明确回执 → 成功关闭”；顺序投递自动复用简化后的单岗位链路。
- 0.5.1 `npm run check` 通过：8 个测试文件共 52 项测试、TypeScript 检查和生产构建成功；测试仅保留明确成功弹窗唯一关闭边界。
- 阶段 34 完成；0.5.1 的状态机简化、测试替换、界面、版本和文档均已按 yuwk 规范自动提交，开发过程未触发真实投递。
- 收到 0.5.1 真实批次反馈：首个 `/job-applied` 页面已经明确投递成功，但结果标签未关闭，批次因此没有回到原岗位列表继续执行。开始阶段 35，只读核对标签元数据与成功页 DOM 后修复跨标签归因。
- Chrome 只读检查时成功标签已关闭，仅能看到原 `/recommend` 列表页；未制造新的真实投递。代码核验确定三处跨标签逻辑都过度依赖 `openerTabId`，下一步改为全局点击前基线与 `/job-applied` 专用候选校验。
- 已新增纯函数归因器：允许成功页没有 `openerTabId`，但必须是点击后新增、路径为 `/job-applied` 且 `number` 与当前岗位编号一致；点击前旧页、其它岗位页和多候选均不会被关闭。
- 成功页正文“恭喜您，投递成功！”现只在 `/job-applied` 专用路径作为强回执；成功页不再挂载完整助手，关闭后后台显式激活冻结的原列表标签。
- 首轮 `npm run check` 通过 9 个测试文件共 58 项测试、TypeScript 检查和生产构建；版本准备升级为 0.5.2 后执行最终回归。
- 最终 `npm run check` 在 0.5.2 下通过：9 个测试文件共 58 项测试、TypeScript 检查和生产构建成功；构建产物已确认包含 `/job-applied` 专用识别且成功页不会自动挂载助手。
- 阶段 35 完成；0.5.2 的跨标签归因、正文回执、关闭/回到原列表、测试、版本和文档均已按 yuwk 规范自动提交。开发与只读诊断均未触发新的真实投递。
- 已实现持久化每日计数、后台任务启动前配额/冷却检查、单批截断、连续成功长冷却倒计时和扩展限流强提示熔断；单岗位入口同样受每日额度与冷却限制。
- 第一轮检查因旧间隔测试比较完整配置而失败，修正断言范围后 `npm run check` 通过 5 个测试文件共 25 项测试、类型检查和生产构建。
- 独立审查未发现配额或冷却可被任务入口绕过；补修跨午夜仍有效的长冷却，并在 `liepinSafety` 存储变化时同步其他侧边栏实例，避免界面显示过期额度。
- 版本升级至 0.2.6；最终 `npm run check` 通过 5 个测试文件共 26 项测试、TypeScript 检查和生产构建，`dist/manifest.json` 版本核对为 0.2.6，阶段 19 完成。
- 用户要求参考 `D:\Github\boss-helper-v2` 支持完全自定义 AI 提示词，并解决 AI 招呼语偶发超过 150 字导致发送被阻止的问题；开始阶段 20。
- 对照确认当前插件提示词完全硬编码；`boss-helper-v2` 提供可编辑角色消息模板和变量渲染，但超长时仍直接跳过。本轮将实现完整业务提示词模板、白名单变量预览、一次 AI 自动压缩和最终保守截断。
- 已定位需要同步修改的配置类型、默认值、旧配置回读、后台保存、AI 请求、侧边栏表单与测试；保留 API Key 隔离和发送前硬校验不变。
- 已增加 `promptTemplate` 配置和默认完整模板，侧边栏提供多行编辑、变量说明及恢复默认模板；旧版存储缺字段时自动迁移到默认模板。
- AI 生成改为渲染白名单变量并追加固定平台边界；首次结果超长或超过 3 句时自动请求压缩，压缩失败或仍超限时对首次可用文本做仅删除尾部的保守收口。
- 首轮 `npm run check` 通过 5 个测试文件共 30 项测试、TypeScript 检查和生产构建；随后补充提示词界面样式、README 变量说明并将版本升级为 0.2.7。
- 最终 `npm run check` 在 0.2.7 下再次通过 5 个测试文件共 30 项测试、TypeScript 检查和生产构建，`dist/manifest.json` 版本核对为 0.2.7，阶段 20 完成。
- 0.2.7 真实批次验收在简历摘要完整时仍报“AI 未生成可用招呼语”；开始阶段 21，优先排查本轮新增请求参数对 GLM 推理模型正文输出的影响，并修正误导错误文案。
- 版本对比确认 0.2.7 新增的固定 `max_tokens: 256` 是此前成功路径不存在的唯一输出额度限制；已移除该参数，并把空正文与模型主动返回“需人工判断”拆成两条准确错误。
- 第一轮 `npm run check` 通过 5 个测试文件共 30 项测试、TypeScript 检查和生产构建；请求体测试明确保证不再携带 `max_tokens`。
- 版本升级至 0.2.8；最终 `npm run check` 再次通过 5 个测试文件共 30 项测试、TypeScript 检查和生产构建，`dist/manifest.json` 版本核对为 0.2.8，阶段 21 完成。
- 用户反馈单岗位内部“移动页面 → 聊一聊 → 招呼 → 发简历 → 确认 → 关闭聊天”几乎瞬时连续执行，而岗位间等待偏长；开始阶段 22，把随机等待下沉到不可逆动作边界，并在保留每日额度/长冷却的前提下适度缩短岗位间默认等待。
- 已确认根因：内容脚本内部只有 80–180ms 的渲染轮询等待，随机 15–45 秒仅位于完整岗位之间；设计为新增可配置动作间隔并通过消息快照传给 Content Script，默认动作间隔 1.5–3.5 秒、岗位间隔 5–15 秒。
- 已扩展批量配置与侧边栏：新增 0.5–10 秒动作间随机等待配置，单岗位预览确认和批量任务都会把保存时的区间快照传给 Content Script；岗位间默认等待调整为 5–15 秒。
- 已在内容脚本六个动作边界接入可中断随机等待，并将简历弹窗轮询改为支持异步等待后重新定位安全确认按钮；首轮 `npm run check` 通过 5 个测试文件共 31 项测试、类型检查和生产构建。
- 复核发现 10 秒动作上限可能让完整闭环超过原 1 分钟看门狗，需同步把失联判定窗口调整为 3 分钟，避免正常节奏被误中断。
- 已把后台任务看门狗放宽为 3 分钟，并在每次等待后的不可逆点击前重新检查停止、安全验证、登录状态及当前按钮节点；旧版缺少动作字段且仍为 15–45 秒默认值的配置会一次性迁移到新 5–15 秒岗位间隔。
- 第二轮 `npm run check` 通过 5 个测试文件共 32 项测试、TypeScript 检查和生产构建；新增测试覆盖动作区间边界、随机端点和旧默认配置迁移。
- 已为简历确认回执额外预留动作最长等待预算，补充 README 的六段动作边界、默认值与合规边界说明，并将版本升级为 0.2.9。
- 最终 `npm run check` 在 0.2.9 下通过 5 个测试文件共 32 项测试、TypeScript 检查和生产构建，`dist/manifest.json` 版本核对为 0.2.9，阶段 22 完成。
- 用户询问能否取消 Chrome Side Panel，参考 `boss-helper-v2` 把完整插件界面注入猎聘首页；开始阶段 23，只读评估技术可行性和风险，不修改业务代码、不执行真实投递。
- 初步确认两个项目都使用 Shadow DOM 隔离网页样式；当前项目已注入悬浮入口，完整 React UI 可复用现有后台消息协议改为页内挂载。
- 已定位主要迁移点：React 入口和 CSS 打包、Side Panel 专用 `chrome.tabs` 调用、动态权限申请、Shadow DOM 弹层/确认框承载、页面重挂载与显式卸载；投递 Content Script 与后台状态机可继续复用。
- 风险初步分级：Shadow DOM 固定浮层方案为中低风险；直接嵌入并挤压猎聘岗位布局为中高风险；账号风控不因 UI 载体变化而明显增加，仍取决于投递频率和页面自动化行为。
- 完成阶段 23：推荐复用现有 `sidepanel.html`，通过仅猎聘域可访问的 iframe 注入 Shadow DOM 固定抽屉；该方案可保留现有 React、权限申请和后台消息逻辑，避免直接 Content Script 化造成标签页/权限回归。
- 本轮只修改评估记录，没有改动 Manifest、构建脚本或运行时代码，也没有触发真实岗位沟通、消息或简历发送。
- 用户确认执行首页注入改造；开始阶段 24，按 Shadow DOM 固定抽屉 + 扩展 iframe 方案实施，投递状态机保持不变。
- 已通过 Chrome 官方文档确认 MV3 的 web-accessible resource、`runtime.getURL()` 导航和 `action.onClicked` 路径均支持该设计。
- 已完成运行时落点核验：Content Script 负责 Shadow DOM 抽屉和开关消息，后台移除 Side Panel 分支并接管工具栏点击，现有 Vite/React 页面继续作为 iframe 内容。
- 已完成 0.3.0 首轮代码改造并新增 3 项抽屉测试；首次检查仅因测试未声明 jsdom 环境失败，业务代码尚未进入构建阶段，已定向补充测试环境声明。
- 第二轮 `npm run check` 通过：6 个测试文件共 35 项测试、TypeScript 检查和生产构建均成功；源 Manifest 与 `dist/manifest.json` 均已移除 Side Panel 并只向猎聘开放 iframe 资源。
- 尝试在真实 Chrome 做纯界面验收时，控制通道无法接管 `chrome://extensions` 内部页面，因此未能代替用户点击重新加载；没有刷新猎聘、点击岗位、发送消息或简历。
- 静态复核补充了收起语义：关闭抽屉仅隐藏并保留 iframe 状态，不会暗中停止批次；按钮提示和 README 已明确要求使用“停止顺序投递”终止运行。
- 最终 `npm run check` 在 0.3.0 下通过：6 个测试文件共 35 项测试、TypeScript 检查和生产构建成功；额外 Manifest/资源校验确认版本、资源路径和 Side Panel 移除状态正确，阶段 24 完成。
- 收到 0.3.0 真实界面反馈：抽屉宽度不足且出现横向滚动；MiniMax-M3 偶发触发“需人工判断”并停止批次。开始阶段 25，增加安全诊断日志和发送前兜底招呼语。
- 已确认“需人工判断”由固定系统提示词与后置校验器的矛盾直接造成，并对照 MiniMax 官方 OpenAI 兼容文档确认标准正文仍位于 `choices[0].message.content`；下一步修改提示词、包装文本提取与诊断记录。
- 已完成抽屉加宽、布局收缩约束、可配置兜底招呼语、两级 POST 诊断和 JSON/代码块/think 标签正文清理；首轮 38 项测试通过，构建仅因测试回调意外返回数组长度产生 TypeScript 签名错误，已定向修正。
- 修正测试签名后完整检查通过；差异审查进一步补上响应读取失败日志和响应意外回显 Key 的二次脱敏，并将版本提升至 0.3.1、更新 README 与抽屉宽度断言。
- 最终 `npm run check` 在 0.3.1 下通过：6 个测试文件共 39 项测试、TypeScript 检查和生产构建成功；`dist/manifest.json` 版本为 0.3.1，构建后的 Content Script 包含 520px 抽屉与同步入口偏移，阶段 25 完成。
- 用户确认猎聘部分基本收束，要求评估与 `D:\Github\boss-helper-v2` 合并为单一插件；开始阶段 26，仅做架构与迁移评估，智联延后，不改写 Git 历史。
- 完成两个仓库的 Manifest、WXT/Vite、Vue/React、后台、Content Script、消息、存储、AI、IndexedDB、测试、产物体量、许可证与 Git 发布链对比；确认 WXT 官方支持多 Content Script、unlisted iframe page 和额外 React Vite plugin。
- 发现合并前置冲突：猎聘对整个 `storage.local` 设置 `TRUSTED_CONTEXTS`，会阻断 BOSS Content Script 直接存储；推荐先把 BOSS 存储和 chat-monitor 入队迁到 Background，再统一保护本地存储。
- 阶段 26 完成：推荐在 BOSS 基线的新 worktree 中实现统一插件，首期保留两个平台各自 UI/状态机；先做配置迁移与 Chrome 双平台验收，Edge/Firefox 与 Vue/AI 统一后置，智联最后实施。本轮未修改 BOSS 仓库，也未触发真实投递。
